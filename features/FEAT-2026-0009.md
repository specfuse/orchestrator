---
correlation_id: FEAT-2026-0009
state: done
involved_repos:
  - Bontyyy/orchestrator-api-sample
autonomy_default: review
task_graph:
  - id: T01
    type: implementation
    depends_on: []
    assigned_repo: Bontyyy/orchestrator-api-sample
    required_templates: [api-controller, api-request-validator]
  - id: T02
    type: qa_authoring
    depends_on: []
    assigned_repo: Bontyyy/orchestrator-api-sample
    required_templates: [test-plan]
  - id: T03
    type: qa_execution
    depends_on: [T01, T02]
    assigned_repo: Bontyyy/orchestrator-api-sample
    required_templates: []
  - id: T04
    type: qa_curation
    depends_on: [T03]
    assigned_repo: Bontyyy/orchestrator-api-sample
    required_templates: []
---

## Description

Adds a bulk-create endpoint `POST /widgets/bulk` to the existing widget API so that clients can create up to 50 widget objects in a single request. Validation is atomic: if any widget in the batch fails validation, none are persisted.

## Scope

- A single new endpoint `POST /widgets/bulk` on the existing `WidgetsController`.
- The request body accepts a JSON array of widget objects; each object must supply `name`, `sku`, and `quantity`.
- The maximum batch size is 50 items. Requests with more than 50 items are rejected before any validation or persistence occurs.
- Validation is atomic — if any widget in the batch fails field-level validation, the entire batch is rejected and zero widgets are persisted.
- On success, returns HTTP 201 with a JSON array of created widget objects, each including a generated `id`.
- On batch-size overflow, returns HTTP 400 with `error.code = "batch_size_exceeded"`.
- On any validation failure, returns HTTP 422 with `error.code = "batch_validation_failure"` and an `error.failures` array (each entry: `index` and `reason`).

## Out of scope

- Bulk update and bulk delete operations.
- Streaming or asynchronous batch processing.
- Idempotency keys or deduplication.
- Partial success responses (all-or-nothing; no 207 Multi-Status).

## Related specs

- `product/features/FEAT-2026-0009.md` — feature narrative with acceptance criteria (AC-1 through AC-3).
