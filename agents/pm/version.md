# PM agent version

Current version: **1.6.3**

**Frozen as the Phase 2 baseline on 2026-04-23 (WU 2.15).** Changes during Phase 3+ require architectural justification. Source of record: [`docs/walkthroughs/phase-2/retrospective.md`](../../docs/walkthroughs/phase-2/retrospective.md) §"Phase 2 freeze declaration".

## Changelog

- `1.6.3` — WU 3.11: two Phase 3 retrospective absorptions on PM surfaces. **F3.6 — inline per-type event schema cross-references.** `agents/pm/CLAUDE.md` §"Output artifacts and where they go" now carries inline schema references for every PM-emitted event type that has a per-type schema: `feature_state_changed` → [`shared/schemas/events/feature_state_changed.schema.json`](../../shared/schemas/events/feature_state_changed.schema.json); `template_coverage_checked` → [`shared/schemas/events/template_coverage_checked.schema.json`](../../shared/schemas/events/template_coverage_checked.schema.json); `human_escalation` → [`shared/schemas/events/human_escalation.schema.json`](../../shared/schemas/events/human_escalation.schema.json). Envelope-only event types (`task_graph_drafted`, `plan_ready`, `task_created`, `task_ready`) are flagged with pointers to their prose-documented payload shapes in [`skills/issue-drafting/SKILL.md`](skills/issue-drafting/SKILL.md) §"Event payloads" and [`skills/dependency-recomputation/SKILL.md`](skills/dependency-recomputation/SKILL.md) §"Event payload — `task_ready`"; F3.15 deferral to Phase 4+ is cited. The CLAUDE.md's closing reference to `verify-before-report.md` §3 is enriched to name the WU 3.11 absorptions. **F3.14 — task lifecycle event payload standardization on `issue_url`.** `skills/issue-drafting/SKILL.md` (v1.3 → v1.4): `task_created` payload now names `issue_url` as the canonical full-URL identifier while retaining `issue` (bare form) for backward compatibility with pre-WU-3.11 consumers; `task_ready` payload (no-dep case only) switches to `issue_url` exclusively. Both worked examples (.NET FEAT-2026-0002/T01 and Python FEAT-2026-0004/T03 + `deliverable_repo`) updated to reflect the new `task_ready` shape. A standardization note cross-references `verify-before-report.md` §3 and cites F3.14's origin (F1 Session 13 burned a verification cycle on the `issue`-vs-`issue_url` asymmetry between `task_started` and `task_created`/`task_ready`). `skills/dependency-recomputation/SKILL.md` (v1.0 → v1.1): `task_ready` payload switches from `issue` to `issue_url`; construct-step sample and worked example updated; payload definition prose cites the standardization rationale. Both SKILLs' construction samples now also reference the verify-before-report.md §3 canonical `--file` invocation and canonical safe append pattern from WU 3.11 where relevant. **Phase 2 freeze-compatibility:** authorized as post-freeze additive correction analogous to WU 3.8 and WU 3.10 on these same surfaces. Purely additive on the CLAUDE.md (inline cross-reference links + one clarifying sentence on envelope-only semantics + pointer enrichment). Field-rename within payload samples on the two skills (migration note retains `issue` on `task_created` for backward-compat; `task_ready` payload rename is authorized because no per-type schema exists and no external consumer has been observed to depend on the old field in walkthroughs to date). No existing step body, event envelope contract, or consumer contract is modified. Phase 2 v1.6.0 baseline contract preserved. Patch-level bump. Closes F3.6 and F3.14 of the Phase 3 retrospective for PM surfaces. Doc-only change; no events emitted.
- `1.6.2` — WU 3.10: absorbed three Phase 3 retrospective findings on cohesive PM surfaces — `agents/pm/skills/issue-drafting/SKILL.md` (v1.2 → v1.3) and `shared/templates/work-unit-issue.md` (v1.1 → v1.2). **F3.4 (2-feature evidence: F1 S12/S14 + F2 S20 both silently skipped the `generating → in_progress` transition and retroactively emitted with trigger `first_round_issues_opened_retroactive` during walkthrough wrap)** — the PM CLAUDE.md already declared PM owned this feature-state transition, but no skill operationalized it. WU 3.10 adds a new `§Step 12 — Feature-state transition check` to issue-drafting/SKILL.md: on every invocation, after §Step 10 (task_created) and §Step 11 (task_ready for no-dep), the skill re-reads the feature's event log fresh and emits `feature_state_changed(generating → in_progress, trigger: "first_round_issues_opened")` iff (a) at least one `task_created` for the feature exists on the log (the one just appended counts), and (b) no prior `feature_state_changed(generating → in_progress)` exists on the log. Guard (b) is the load-bearing idempotence — only the first invocation emits; subsequent invocations no-op. v1 operational semantics: "first round" = "first invocation to successfully append a task_created"; Phase 4+ may tighten to "all tasks in task_graph have task_created" if walkthrough experience dictates (documented as a Phase 4+ carry-note inside the step itself). Event construction follows the same `scripts/validate-event.py` + `source_version` + append + re-read discipline as other emissions; envelope + per-type schema at `shared/schemas/events/feature_state_changed.schema.json` already supports this payload shape (no schema change). §Scope's emission-list bullet extended to name the new emission. §"Outputs" closing line corrected (previously stated "No other state transitions on the feature" — replaced with "The only feature-state transition the skill owns is `generating → in_progress`, emitted via §Step 12; no other feature-level transitions are performed by this skill"). §"Verification — issue-creation path" updated to assert Step 12 ran determinately (either emitted the transition or was a no-op under the idempotence guard). **F3.7 (2-feature evidence: F1 S4 + F2 S4 both needed explicit preamble pinning to override the SKILL's concrete `clabonte/orchestrator` target in the worked example)** — replaced every `clabonte/orchestrator` reference in the `Worked example — FEAT-2026-0004/T03` section with the generic placeholder `<product-specs-owner>/<product-specs-repo>`; added a historical-reconstruction note at the top of the example explaining the Phase-2-era target was pre-specs-repo. `shared/templates/work-unit-issue.md` template comments updated: `<product-specs-owner>/<product-specs-repo>` replaces the Phase-2-era `clabonte/orchestrator` concrete target (three occurrences — example comment, frontmatter field semantics note, `## Deliverables` section comment); `orchestrator repo` prose softened to `product specs repo` where it refers to the deliverable destination; v1.2 changelog block added to the top comment. Comment-only change on the template; no YAML structure, frontmatter field, or mandatory section is modified; fully backwards-compatible with v1.1 issues. **F3.29 (F2 S4 fell back to registry-derived work-unit prompts when no plan file existed — worked for walkthrough continuity but undocumented)** — §Step 2 gains a "Plan-file fallback" paragraph: if `/features/<feature_correlation_id>-plan.md` does not exist on the orchestration repo's current HEAD, the skill falls back to deriving work-unit prompts from the feature registry's per-AC prose (`## Description` + `## Scope` + `## Acceptance criteria`), logs the deviation as `plan_file_fallback: true` in the drafting transcript, and **escalates `spec_level_blocker`** in production when the task graph is non-trivial (more than one task, or any task with non-empty `depends_on`) because the plan file is the mechanism by which the human validates the decomposition. For single-task simple features, the fallback is acceptable without escalation. **Phase 2 freeze-compatibility:** authorized as post-freeze additive correction analogous to WU 3.8 on the component agent and WU 3.12 on task-decomposition — purely additive on the skill (new §Step 12; new §Scope bullet; §Step 2 fallback documentation; corrected §Outputs / §Verification prose that was out of sync with the role-level ownership assertion in PM CLAUDE.md) and on the template (comment-only, no YAML / frontmatter / section-structure change). No existing step body, event contract, schema, or consumer contract is modified. PM CLAUDE.md's pre-existing declaration that PM owns the `generating → in_progress` transition is preserved — the skill now operationalizes the documented ownership. Patch-level bump to signal post-freeze additive correction rather than minor feature addition. Doc-only operational additions; no new schemas, no event type additions (uses the existing `feature_state_changed` contract), no frozen Phase 1 surface touched, no QA or component skill modified, no shared rule modified, no agents/pm/CLAUDE.md edit. Closes Phase 3 retrospective findings F3.4, F3.7, F3.29.
- `1.6.1` — WU 3.12: absorbed Phase 3 retrospective finding F3.9 into `skills/task-decomposition/SKILL.md` (v1.1 → v1.2). F1 Step 1 subagent noted momentary step-count confusion between the SKILL's 8 numbered procedure steps and a 7-numbered downstream consumer (most plausibly the T01 work-unit-issue body's `## Verification` section). Direct file inspection confirms the current SKILL.md does not contain a literal "7 steps" phrase (git archeology: two commits only, WU 2.2 initial + WU 2.10 clarifications — neither introduced such a phrase), so F3.9's spirit (prevent step-count miscount) is honored via an additive positive count assertion rather than a text replacement. Added an opening clarifying sentence immediately under §"The decomposition procedure" heading (before §Step 1): "**The procedure consists of 8 numbered steps** (Step 1 through Step 8). Downstream references — in the work unit issue body's `## Verification` section, in `../../CLAUDE.md` §'Role-specific verification', or in any other consumer — should use this SKILL's step numbering as the canonical source." followed by an enumeration of the 8 step titles for explicit disambiguation. **Phase 2 freeze-compatibility:** authorized as post-freeze additive correction analogous to WU 3.8 on the component agent (Phase 1 v1.5.0 → v1.5.1 pattern). The amendment is **purely additive** — no existing step, step number, step ordering, or step body is modified; no schema, event, or consumer contract is touched; the clarifying sentence is a positive restatement of what the file already contains. Phase 2 v1.6.0 baseline contract is preserved. Patch-level bump (not minor) to signal post-freeze additive correction rather than feature addition. No other files in this role touched. Doc-only change; no events emitted. Closes Phase 3 retrospective finding F3.9 (trivial-clarity bucket; not Phase 4 gating).
- `1.6.0` — WU 2.14: shared substrate + scripts hygiene. (a) F1.5+F2.6: `source_version` human-event convention (commit SHA vs. `n/a`) promoted to `shared/rules/verify-before-report.md` §3 as an explicit clause — prefer commit SHA for versioned orchestration-repo actions, `n/a` for unattached/test-time emissions. Additive-only; no existing events invalidated. (b) F1.6: `scripts/validate-event.py` help text updated — explicit "Supported invocation patterns" in docstring, `--stdin` alias added (accepts stdin explicitly, same behaviour as omitting `--file`), unsupported args rejected with a pointer to the two supported patterns. (c) F1.7: zsh glob-expansion hazard in `template-coverage-check/SKILL.md` sample commands fixed — all three `gh api ...?ref=main` URLs now single-quoted to prevent `?` glob expansion under zsh. Skill internal version unchanged (docstring-only). (d) F1.8: `scripts/requirements.txt` extended to declare `pyyaml>=6.0` alongside existing `jsonschema>=4.18`; `scripts/validate-frontmatter.py` created — parallel to `validate-event.py`, accepts `--file <path>` or stdin/`--stdin`, parses YAML frontmatter from markdown files and validates against `shared/schemas/feature-frontmatter.schema.json`; `scripts/README.md` created with bootstrap instructions and docs for all three scripts. (e) F2.4: provenance `$comment` fields added to `shared/schemas/event.schema.json`, `shared/schemas/feature-frontmatter.schema.json`, and all four per-type schemas under `shared/schemas/events/`, naming which WU introduced which field/enum value. (f) Does NOT add `plan_reingested` event type — WU 2.11's `template_coverage_checked` re-chain emission on every Phase B success already makes Phase B success observable in the event log; adding `plan_reingested` would be redundant with that signal. Additive-only on Phase 1 frozen substrate per WU 2.5 precedent.
- `1.5.0` — WU 2.13: issue-drafting skill + work-unit template polish. (a) `shared/templates/work-unit-issue.md` bumped to v1.1: optional `deliverable_repo` frontmatter field added — when absent, defaults to `component_repo`; when present, names the repo where the task's primary deliverable lives (e.g. `clabonte/orchestrator` for orchestrator-hosted test plans); §Verification commands that operate on the deliverable run from `deliverable_repo`'s root while build/test commands still run from `component_repo`'s root. Closes finding F1.3 (part 1). (b) Optional `## Deliverables` section added to `work-unit-issue.md` template between `## Context` and `## Acceptance criteria`: when present, names each file the task produces by its relative path from `deliverable_repo`'s root; when absent (most implementation tasks), no change to issue structure. Closes finding F1.3 (part 2). Both additions are backwards-compatible: issues authored before v1.1 (without `deliverable_repo`, without `## Deliverables`) remain structurally valid. (c) `issue-drafting/SKILL.md` bumped to v1.2: Step 3 extended with a `deliverable_repo` and `## Deliverables` clause — for QA authoring/curation tasks whose deliverable lives outside `component_repo`, set `deliverable_repo` in frontmatter and populate `## Deliverables`; for implementation tasks, omit both. Annotates `§Verification` with which repo each command block runs against. Closes finding F1.3 integration. (d) Second worked example added to `issue-drafting/SKILL.md` — FEAT-2026-0004/T03 (`qa_authoring`) against `Bontyyy/orchestrator-api-sample` / deliverable to `clabonte/orchestrator`: Python stack, `deliverable_repo` + `## Deliverables` in use, `pytest`/`mypy`/`ruff` verification mechanics, claim surface of absence checks rather than presence checks. Demonstrates both the non-.NET stack and the QA deliverable split coherently. Closes finding F1.4.
- `1.4.0` — WU 2.12: plan-review skill polish. (a) Structural-vs-prose edit distinction clarified in §"Two edit surfaces": structural edits (YAML-block changes: task additions/removals, `assigned_repo`, `depends_on`, `required_templates` changes) trigger Phase B re-ingest; prose edits (`### Work unit prompt` bodies) are the primary human authoring task and do not trigger re-ingest structurally. The "no edits during plan review" criterion in WU 2.7 acceptance criteria refers to structural edits only. Closes finding F1.13. (b) Stale-heading behavior documented explicitly in §"Two edit surfaces": heading lines are regenerated on Phase A emission only; Phase B leaves the plan file untouched; heading lines can therefore become stale between Phase A emissions when Phase B accepts YAML-block changes; the YAML block is canonical and is what consumers (human, issue-drafting skill) read from. Worked example note corrected to remove the incorrect "next re-ingest will regenerate" claim. Closes finding F2.12. (c) Human-authoring sequence during `plan_review` made explicit in new §"Human-authoring steps during `plan_review`" sub-section (placed after Phase A exit criteria): enumerates the 5-step sequence from Phase A emission through `required_templates` population (structural edit, triggers re-chain pre-flight) through prose drafting through approval signaling. First-time reader no longer needs to cross-reference Out-of-scope clauses in two other skills to reconstruct the sequence. Closes finding F1.2. Skill internal version bumped: `plan-review/SKILL.md` v1.1 → v1.2.
- `1.3.0` — WU 2.11: template-coverage skill contract hardening + plan-review Phase B chaining. (a) `template-coverage-check/SKILL.md` Out-of-scope clause rephrased as imperative prohibition: "DO NOT populate `required_templates` even if absent; escalate `spec_level_blocker` rather than infer the field." New Step 2.5 (pre-flight check) added: if any task in the feature's task graph is missing the `required_templates` key entirely (field absent — distinct from explicit empty array `[]`), the skill immediately escalates `spec_level_blocker` with a `human_escalation` event + inbox file naming the tasks; `qa_execution`/`qa_curation` tasks that carry `required_templates: []` are not escalated. Closes finding F2.3. (b) `plan-review/SKILL.md` Phase B extended with §"Template-coverage re-chain" final step: after every successful structural validation, Phase B unconditionally re-invokes the full template-coverage-check procedure (pre-flight + coverage walk) against the post-edit graph. On pass, emits a fresh `template_coverage_checked` event (validated through `scripts/validate-event.py` before append). On pre-flight failure or coverage gap, escalates `spec_level_blocker` via the same path as primary coverage-check invocations. Phase B Outputs, Verification, exit-criteria, Scope, and "What this skill does not do" sections all updated to reflect the re-chain. Closes findings F2.5 and F2.7. (c) Event emission discipline: every Phase B success now leaves a `template_coverage_checked` event in the log (unconditional, not conditional on change detection), making coverage status at each re-ingest observable in the audit trail. Skill internal versions bumped: `template-coverage-check/SKILL.md` v1.0 → v1.1; `plan-review/SKILL.md` v1.0 → v1.1.
- `1.2.0` — WU 2.10: task-decomposition skill clarifications and issue-drafting prose-narration clause. (a) `task-decomposition/SKILL.md` Step 4: added "Feature scope overrides" subsection — when the feature `## Scope` explicitly constrains `qa_authoring` cardinality (e.g. "one authored test plan covering both behaviors"), that constraint overrides the default per-behavior count; the default applies only when `## Scope` is silent on cardinality. Closes findings F1.9, F2.1. (b) `task-decomposition/SKILL.md` Step 5 rule 3: rewrote `qa_execution depends_on` to use a same-behavior gate rather than a whole-repo gate — `qa_execution` depends on the implementation tasks for the same behavior as its matched `qa_authoring`, not all implementation tasks on the repo; cross-repo impl tasks mocked at the test boundary are excluded; includes worked example showing 2-behavior same-repo feature. Closes findings F1.10, F2.2. (c) `task-decomposition/SKILL.md` `decomposition_pass` payload field: added explicit statement that the counter is not persisted in the feature frontmatter and is derived by counting prior `task_graph_drafted` events in the event log at emission time. Closes finding F1.11. (d) `task-decomposition/SKILL.md` Step 7: added orphan-check asymmetry note — adding a task risks orphans on the new task's own `depends_on`; removing a task risks orphans in any surviving task's `depends_on`. Folds positive observation F2.13 into skill text. (e) `issue-drafting/SKILL.md` Step 3: added upstream-task narration clause — for any task with non-empty `depends_on`, both the YAML `depends_on` field and a human-readable prose mention in `## Context` are required. Closes finding F1.12.
- `1.1.0` — WU 2.9: event schema additions — `feature_state_changed` and `human_escalation` per-type payload. (a) `feature_state_changed` emission points named explicitly in `CLAUDE.md` §"Output artifacts": `planning → plan_review` (`trigger: plan_ready`), `plan_review → generating` (`trigger: plan_approved`), `generating → in_progress` (`trigger: first_round_issues_opened`), `in_progress → done` (`trigger: all_tasks_done`). (b) New per-type payload schemas added at `shared/schemas/events/feature_state_changed.schema.json` (fields: `from_state`, `to_state`, `trigger`) and `shared/schemas/events/human_escalation.schema.json` (fields: `summary`, `reason` enum, optional `cycle_members` array). (c) F2.9 escalation-state tension resolved in §"Output artifacts": `human_escalation` event is the authoritative blocked signal; PM agent does NOT write to feature frontmatter `state` on escalation. Closes findings F1.1, F2.8, F2.9.
- `1.0.0` — WU 2.1: PM agent configuration rewritten to production v1 quality. `CLAUDE.md` restructured around the five Phase 2 skills (task decomposition, plan review, issue drafting, dependency recomputation, template coverage), with role-specific verification clauses pointing to each skill, an anti-patterns section codifying the PM-side failure modes from Phase 1 walkthroughs, explicit output-artifact definitions, and a preserved "Phase 2 specification inputs" list for any additional inherited specs that later Phase 2 WUs may append. Finding 6 of the Phase 1 walkthrough retrospective (shared-rules re-read discipline at role-switch) absorbed into the new shared rule `shared/rules/role-switch-hygiene.md` rather than duplicated per-role, so every operational role (component, QA, specs, future meta-agents) inherits the discipline without drift. The shared edit is authorized against the Phase 1 freeze by the retrospective's explicit carve-out for Finding 6. `agents/pm/README.md` added for cold-open navigation. `agents/pm/rules/` remains empty by design; no role-specific overrides are needed at v1. Closes WU 2.1 of Phase 2.
- `0.2.0` — WU 1.9: added forward specification `issue-drafting-spec.md` as an inherited contract for the Phase 2 WU that will author the issue-drafting skill. The skill must re-verify every claim about target-repo state at drafting time and capture the verification in a durable surface. Added pointer from `CLAUDE.md` under new "Phase 2 specification inputs" section. Closes Finding 3 of the Phase 1 walkthrough retrospective — the last Fix-in-Phase-1 before freeze.
- `0.1.0` — Initial Phase 0 draft.
