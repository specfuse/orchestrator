# Component agent version

Current version: **1.5.2**

**Frozen as the Phase 1 baseline on 2026-04-22 (WU 1.12).** Changes during Phase 2+ require architectural justification. Source of record: [`docs/walkthroughs/phase-1/retrospective.md`](../../docs/walkthroughs/phase-1/retrospective.md) §"Phase 1 freeze declaration".

## Changelog

- `1.5.2` — WU 3.11: F3.6 absorption — `agents/component/CLAUDE.md` §"Output artifacts and where they go" now carries inline per-type event payload schema cross-references for every event type the component role emits that has a schema: `task_started` → [`shared/schemas/events/task_started.schema.json`](../../shared/schemas/events/task_started.schema.json); `human_escalation` → [`shared/schemas/events/human_escalation.schema.json`](../../shared/schemas/events/human_escalation.schema.json). Envelope-only event types (`task_completed`, `task_blocked`, `override_applied`, `override_expired`, `spec_issue_raised`) are flagged as "envelope-only at v1.5.2" with a pointer to the Phase 3 retrospective's F3.15 deferral of per-type schemas for this set to Phase 4+. The CLAUDE.md's closing reference to `verify-before-report.md` §3 is enriched to name the WU 3.11 absorptions (`--file /tmp/event.json` canonical invocation, JSONL single-line requirement, canonical safe append pattern, timestamp discipline). Closes Phase 3 retrospective finding F3.6 for the component role. **Phase 1 freeze-compatibility:** authorized as post-freeze additive correction analogous to WU 3.8 on this same surface. Purely additive — inline cross-reference links to existing schema files, plus one clarifying sentence on envelope-only semantics, plus a pointer enrichment on a verify-before-report.md cross-reference that already existed. No existing content rewritten; no schema modified; no event type added or removed; no role-responsibility change. Phase 1 v1.5.0 baseline contract preserved. Patch-level bump. No other files in this role touched. Doc-only change; no events emitted.
- `1.5.1` — WU 3.8: absorbed Phase 1 walkthrough retrospective Finding 8 into `skills/verification/SKILL.md` (→ 1.2) by adding a new §"Pre-gate build step" that mandates `dotnet restore && dotnet build` (or the stack-equivalent) before any gate whose command embeds `--no-build` or `--no-restore`. Triggered by 2-feature live empirical evidence from Phase 3 walkthrough WU 3.6 (F1 Step 5 + F2 Step 5; finding F3.1 of the Phase 3 retrospective). **Phase 1 freeze-compatibility:** authorized under the Phase 1 retrospective's own defer language for Finding 8 — "carry into the next edit of `verification/SKILL.md`, opportunistically — no dedicated work unit required." The amendment is **purely additive** — it adds a mandatory prerequisite before gate 1; it does not modify, reorder, or remove any existing gate, gate command, pass condition, or `task_completed` payload shape. The Phase 1 v1.5.0 baseline contract is preserved. Patch-level bump (not minor) to signal post-freeze additive correction rather than feature addition. No other files in this role touched.
- `1.5.0` — WU 1.10: escalation skill `spec_level_blocker` clause now specifies that features without a product specs repo (walkthrough, bootstrap, legacy) route spec issues to the orchestrator repository itself. Bumped `skills/escalation/SKILL.md` (→ 1.2). No other files in this role touched. Closes Finding 4 of the Phase 1 walkthrough retrospective.
- `1.4.0` — WU 1.8: event `source_version` field must now be produced by `scripts/read-agent-version.sh component` at emission time, never eye-cached from an earlier read. Added the helper script (bash, no deps) with exit-code semantics aligned with the WU 1.7 validator (`0` pass, `1` parse fail = failed verification cycle, `2` setup error = escalate). Tightened `shared/rules/verify-before-report.md` §3 with a new bullet; updated `CLAUDE.md` §"Output artifacts" to point at both scripts. Skills untouched — the construction discipline is shared-rule-owned. Closes Finding 2 of the Phase 1 walkthrough retrospective.
- `1.3.0` — WU 1.7: event emission is now gated on machine validation. Added `scripts/validate-event.py` (new top-level infrastructure) and `scripts/requirements.txt`. Tightened `shared/rules/verify-before-report.md` §3 to require `scripts/validate-event.py` exit `0` before any `events/*.jsonl` append, with exit-code semantics (`0` pass, `1` schema violation counts as a failed verification cycle, `2` setup error escalates). Updated `CLAUDE.md` §"Output artifacts" and bumped `skills/verification/SKILL.md` (→ 1.1), `skills/pr-submission/SKILL.md` (→ 1.1), `skills/escalation/SKILL.md` (→ 1.1) to invoke the validator at their respective emission points (`task_completed` in verification and pr-submission; `task_blocked` / `spec_issue_raised` / `human_escalation` in escalation). Closes Finding 1 of the Phase 1 walkthrough retrospective.
- `1.2.0` — WU 1.3: added `skills/pr-submission/SKILL.md` v1 and `skills/escalation/SKILL.md` v1 as the normative outgoing-communication skills for the role. PR submission skill defines the branch-naming convention, Conventional Commits headline with mandatory `Feature: FEAT-YYYY-NNNN/TNN` trailer, PR title and description shape (correlation ID on the first description line), and the ordered `in_progress → in_review` handoff (push → PR → label rotation → `task_completed` event). Escalation skill defines the four reasons (`spinning_detected`, `spec_level_blocker`, `override_expiry_needs_review`, `autonomy_requires_approval`), the trigger / artifacts / state-transition / events flow for each, the internal spinning counter (resets on full green cycle, fires at 3 consecutive failures), and the post-escalation stop discipline. Replaced the 1.0.0 PR+escalation placeholder in `CLAUDE.md` with pointers to both skills.
- `1.1.0` — WU 1.2: added `skills/verification/SKILL.md` v1 as the normative verification skill for the role. Defines the six mandatory gates (tests, coverage ≥ 90%, compiler-warnings, lint, security-scan, build) aligned with architecture §10, the `.specfuse/verification.yml` repo contract, the per-gate and `task_completed` payload shapes, and the failure-handling flow (local correction, spinning detection, spec-level escalation). Replaced the 1.0.0 verification placeholder in `CLAUDE.md` with a pointer to the skill.
- `1.0.0` — Phase 1 v1 baseline. Rewrite from v0.1 Phase 0 draft: tightened role definition with explicit non-responsibilities; enumerated entry transitions owned with ownership rationale; expanded output artifact list to name the exact events the role emits; added explicit anti-patterns section covering the ten failure modes that regress the orchestrator's trust model; added role-specific skill placeholders pointing at `skills/verification/SKILL.md` (WU 1.2) and `skills/pr-submission/SKILL.md` + `skills/escalation/SKILL.md` (WU 1.3); declared `/agents/component/rules/` intentionally empty at v1.0.0 with the override procedure documented. Added `README.md` for directory cold-open.
- `0.1.0` — Initial Phase 0 draft.
