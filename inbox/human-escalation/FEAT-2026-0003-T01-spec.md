## Correlation ID

FEAT-2026-0003/T01

## Reason

spec_level_blocker

## Agent state

- Role: component:orchestrator-api-sample
- What I was doing: picked up [Bontyyy/orchestrator-api-sample#7](https://github.com/Bontyyy/orchestrator-api-sample/issues/7) (`[FEAT-2026-0003/T01] Enforce unique widget name on POST /widgets`), read the issue body, read the current state of `WidgetService.cs`, `IWidgetRepository.cs`, and `Widget.cs` in preparation for implementation.
- What I tried:
  - Searched the component repository for precedent on name-comparison semantics (`grep` for `StringComparison`, `OrdinalIgnoreCase`, `InvariantCulture`, `ToLower`, `ToUpper` across `src/`). No matches. There is no existing convention in this repo that resolves the case-sensitivity question.
  - Re-read acceptance criteria 1 and 3 of the issue body. Criterion 1 says "rejected rather than persisted" but does not specify the HTTP status or body shape. Criterion 3 requires the check to be "against the live state of `IWidgetRepository`" but does not say what method on the port should be added to support it.
  - Inspected the existing validation convention: every input-validation rejection currently uses `ValidationException` → `400 validation_failed`. Extending this convention to duplicate-name collisions is defensible but differs from the REST-standard `409 Conflict` for duplicate-resource semantics on `POST`. I do not have authority to pick between the two.
  - Considered writing an override to proceed with a self-chosen convention; rejected — per `agents/component/CLAUDE.md` anti-pattern 7 and the escalation skill §2, the component agent does not self-authorize an override when the resolution is a product-level decision.
- Relevant links:
  - Task issue: [Bontyyy/orchestrator-api-sample#7](https://github.com/Bontyyy/orchestrator-api-sample/issues/7)
  - Spec issue filed: [clabonte/orchestrator#5](https://github.com/clabonte/orchestrator/issues/5)
  - Feature registry: [`features/FEAT-2026-0003.md`](../../features/FEAT-2026-0003.md)
  - Event log: [`events/FEAT-2026-0003.jsonl`](../../events/FEAT-2026-0003.jsonl) (entries `task_started`, `spec_issue_raised`, `task_blocked`, `human_escalation`)

The three ambiguities are documented in full in the spec issue. Briefly: (1) case sensitivity of the uniqueness comparison is unspecified and has no repo precedent; (2) the HTTP error contract for the rejection is unspecified and has two defensible answers (repo-convention `400 validation_failed` vs REST-standard `409 Conflict`); (3) the repository method signature needed to support the check depends on the resolution of (1).

I have not modified any source file. No feature branch has been pushed on the component repo. Resumable state on unblock: re-pick up the task from `state:ready` after a human amends the issue body (or adds a linked spec) to resolve the three ambiguities.

## Decision requested

How should `FEAT-2026-0003/T01` proceed? Please choose one:

- **(a)** Triage the spec issue on [clabonte/orchestrator#5](https://github.com/clabonte/orchestrator/issues/5), commit the product decisions on the three axes as an amendment to the work-unit issue body (Bontyyy/orchestrator-api-sample#7), then rotate the label back to `state:ready` so I can resume. (Normal unblock path.)
- **(b)** Reshape the task — e.g., split uniqueness-on-create from a separate uniqueness-policy decision task — if you judge the three axes warrant their own upstream discussion.
- **(c)** Abandon T01 and replan if uniqueness is not in fact the right feature to exercise the escalation path on this walkthrough.

Do not instruct me to "just pick a convention and ship" — that is the anti-pattern this edge-case walkthrough is designed to catch (see `docs/walkthroughs/phase-1/task-C-log.md`).
